services:
  openvas-scanner:
    build:
      context: .
      dockerfile: Dockerfile.openvas-scanner
    container_name: openvas-scanner
    volumes:
      - openvas_run:/var/run/openvas
      - openvas_plugins:/var/lib/openvas
    restart: unless-stopped

  ospd-openvas:
    build:
      context: .
      dockerfile: Dockerfile.ospd-openvas
    container_name: ospd-openvas
    volumes:
      - openvas_run:/var/run/openvas
      - ospd_run:/var/run/ospd
      - openvas_plugins:/var/lib/openvas
    depends_on:
      - openvas-scanner
    restart: unless-stopped

volumes:
  openvas_run:
  ospd_run:
  openvas_plugins:
